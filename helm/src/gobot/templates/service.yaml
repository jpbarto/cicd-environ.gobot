apiVersion: v1
kind: Service
metadata:
  name: {{ include "gobot.fullname" . }}
  labels:
    {{- include "gobot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gobot.selectorLabels" . | nindent 4 }}
